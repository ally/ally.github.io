---
title: Ally Long – product design, front-end development
---


<div class="wrapper--intro">
  <div class="container--intro">
  <div class="container__text--intro">
    <p class="text--intro">
      <span class="text__divider--large text--bold">Ally Long</span>
      <% data.content.skills.each do |skill| %>
        <span <%= skill == data.content.skills.last ? "class='last'" : nil %>>
        <% skill_name = skill.split(" ") %>
        <% skill_name.each do |sn| %>
          <%= 
            sn == skill_name.last ? content_tag("span", sn, :class => "text__divider--large") : sn
          %>
        <% end %>
        </span>
      <% end %>
    </p>
    <p class="text--small">
      <span class="text--bold text--prefix">Find me</span>
      <% data.content.contact.each do |c| %>
        <span class="text__divider<%= c == data.content.contact.last ? " last" : nil %>">
          <%= link_to(c.name, c.link) %>
        </span>
      <% end %>
    </p>
  </div>
  <div class="container--profile-img">
    <%= image_tag("/images/allyelle-profile.jpg", :class => "image--profile") %>
  </div>
  </div>
</div>
<div class="container--divider">
</div>

<div class="container--work">
  
  <p>
    I help conceptualise, facilitate, design, prototype and build digital products. I've worked with agencies and international brands, as well as NGOs and non-profits to create useful solutions to complex problems. 
    I have a particular interest in designing for novice tech users and people in emerging economies.
    If you need help figuring out your product, <span class="text--bold"><a href="mailto:ally@allyelle.com">say hello</a></span>.
  </p>

  <p>
    <span class="text--bold text--prefix">I’ve worked for</span>   
    <% data.content.jobs.each do |job| %>
      <span<%= job ==  data.content.jobs.last ? " class='last'" : nil %>>
        <a href="<%= job.link %>">
          <% job_name = job.name.split(" ") %>
          <% job_name.each do |jn| %>
            <span<%= jn == job_name.last ? " class='text__divider--large'" : nil %>><%= jn %></span>
          <% end %>
        </a>
      </span>
    <% end %>
  </p>

  <p>
    <span class="text--bold text--prefix">Clients and partners have included</span>   
    <% data.content.clients.each do |client| %>
      <span<%= client ==  data.content.clients.last ? " class='last'" : nil %>>
        <% client_name = client.split(" ") %>
        <% client_name.each do |cn| %>
        <span<%= cn == client_name.last ? " class='text__divider--large'" : nil %>><%= cn %></span>
        <% end %>
      </span>
    <% end %>
  </p>
</div>

<div class="wrapper__header--talks">
  <div class="container__header--talks">
    <span class="text--hero">Talks & workshops</span>
  </div>
</div>

<div class="wrapper--talks">
  <div class="container--talks">
    <ul class="list--clean">
      <% data.content.talks.each do |talk| %>
        <li>
          <span class="text--bold">
          <% if talk.link.present? %>
            <span><%= link_to(talk.organiser, talk.link) %></span>
          <% else %>
            <%= talk.organiser %>
          <% end %>
          </span>
          <span><%= talk.title %></span>
          <span class="list__item--meta">
            <span><%= talk.date %></span>
            <% location = talk.location.split(" ") %>
            <% location.each do |l| %>
              <% if l == location.last %>
                <%= content_tag("span", l) %>
              <% else %>
                <%= l %>
              <% end %>
            <% end %>
            <% unless talk.video.blank? %>
              <span><%= link_to("video", talk.video) %></span>
            <% end %>
            <% unless talk.slides.blank? %>
              <span><%= link_to("slides", talk.slides) %></span>
            <% end %>
            <% unless talk.photos.blank? %>
              <span><%= link_to("photos", talk.photos) %></span>
            <% end %>
            <% unless talk.background.blank? %>
              <span><%= link_to("background", talk.background) %></span>
            <% end %>
          </span>
        </li>
      <% end %>
    </ul>
  </div>
</div>


<div class="wrapper__header--photos">
  <div class="container__header--photos">
    <span class="text--bold">Selected travel photos – work & play</span>
  </div>
</div>

<div class="wrapper--photos">
  <div class="container--photos">  
    <% data.content.photos.each_slice(3) do |photo_row|%>
      
        <div class="row--photos">
          <% photo_row.each do |photo|%>
            <div class="wrapper__img">
              <%= image_tag(photo.img) %>
              <span class="photo__caption"><%= photo.caption %></span>
            </div>
          <% end %>
        </div>
      
    <% end %>

  </div>
</div>

<div class="wrapper--colophon">
  <div class="container--colophon">
    <p>
      <span class="text__divider">&copy; Ally Long <%= Date.today.year %></span>
      Site created with Middleman
       and hosted on GitHub,
      using Cera PRO typeface by Jakob Runge.
    </p>
  </div>
</div>